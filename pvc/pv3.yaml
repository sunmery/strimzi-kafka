apiVersion: v1
kind: PersistentVolume
metadata:
  name: data-cluster-zookeeper-3
  labels:
    app: kafka
spec:
  capacity:
    storage: 10Gi
  volumeMode: Filesystem
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  storageClassName: nfs-csi
  nfs:
    path: /mnt/data/155/k8s/cluster/zookeeper  # NFS共享的路径
    server: 192.168.2.160  # NFS服务器地址

